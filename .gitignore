# ─────────────────────────────
# Environment files
# ─────────────────────────────
.env
.env.*          # local/test/prod envs
!.env.example   # keep placeholder example in repo
.env.local      # CRA local environment
.env.production # CRA production environment
client/.env     # CRA frontend env (never commit real keys)
client/.env.local
client/.env.production
server/.env

# ─────────────────────────────
# Node / build outputs
# ─────────────────────────────
node_modules/
client/node_modules/
server/node_modules/

build/          # CRA build output
dist/           # Vite/TS build output
client/build/   # Client build files
server/build/   # Server build files
.next/          # Next.js
.out/
.vercel/        # Vercel build artifacts
coverage/

# ─────────────────────────────
# Public assets (CRA vs Gatsby)
# ─────────────────────────────
# Do NOT ignore client/public (needed for CRA builds)
# Ignore root-level public/ (for Gatsby, etc.)
# /public/
# Note: Commented out /public/ to avoid conflicts with client folder
!client/public/
!client/public/**

# ─────────────────────────────
# Tooling & IDEs
# ─────────────────────────────
.vscode/
.idea/
.DS_Store
Thumbs.db
*.swp
*.swo
*~

# ─────────────────────────────
# Logs
# ─────────────────────────────
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# ─────────────────────────────
# Firebase
# ─────────────────────────────
.firebase/
firebase-debug.log
firebase-debug.*.log
*-firebase-adminsdk-*.json
service-account-key.json

# Firebase configuration files (project-specific)
.firebaserc
firebase.json
firestore.rules
firestore.indexes.json
storage.rules
functions/

# Keep Firebase client config as examples only
!firebase.example.json
!firestore.example.rules

# ─────────────────────────────
# Stripe
# ─────────────────────────────
stripe-webhook-secret.txt

# ─────────────────────────────
# Caches
# ─────────────────────────────
.cache/
.parcel-cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/
.nyc_output

# ─────────────────────────────
# Misc / security
# ─────────────────────────────
*.pem
*.key
*.crt
*.p12
*.pfx
*.db
*.sqlite
*.sqlite3
*.bak
*.backup
*.zip
*.tar.gz
*.rar
tmp/
temp/
