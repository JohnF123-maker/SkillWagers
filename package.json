{
  "name": "skillwagers",
  "version": "1.0.0",
  "description": "SkillWagers - Skill-based gaming platform with escrow and dispute resolution",
  "main": "index.js",
  "scripts": {
    "dev": "npm run client",
    "client": "cd client && npm start",
    "build": "cd client && npm run build",
    "build-functions": "cd functions && npm run build",
    "install-client": "cd client && npm install",
    "install-functions": "cd functions && npm install",
    "install-all": "npm run install-client && npm run install-functions"
  },
  "keywords": [
    "gaming",
    "esports",
    "marketplace",
    "escrow"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "firebase": "^12.3.0"
  }
}
